import { createSlice } from "@reduxjs/toolkit";

const initalState = {};

export const invitePatronsSlice = createSlice({
  name: "invite_patrons",
  initialState: initalState,
  reducers: {},
});

export const {} = invitePatronsSlice.actions;

export default invitePatronsSlice.reducer;
