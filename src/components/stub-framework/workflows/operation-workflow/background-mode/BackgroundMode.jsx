import React from "react";
import Container from "react-bootstrap/Container";
import {Gallery} from "@ditype/react-commons";

function BackgroundMode({
  data,
  metadata,
  configuration,
  customisation,
  contextManager,
}) {
  return (
    <>
    </>
  );
}
export default BackgroundMode;
